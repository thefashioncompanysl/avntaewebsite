import * as React from 'react';

export const BorderBeam = (_props: { duration?: number; size?: number }) => {
    // Decorative stub to match the provided component import.
    // Kept intentionally minimal so it doesn't alter existing colors or layout.
    return null;
};

export default BorderBeam;
